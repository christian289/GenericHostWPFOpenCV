<UserControl x:Class="OhMyRudolph.Wpf.UserControls.DetectingNoseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cnvt="clr-namespace:OhMyRudolph.Wpf.Core.Converters;assembly=OhMyRudolph.Wpf.Core"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OhMyRudolph.Wpf.UserControls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance local:DetectingNoseViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <Grid x:Name="grid">
        <Image Width="{Binding ActualWidth, ElementName=grid, Mode=OneWay}"
               Height="{Binding ActualHeight, ElementName=grid, Mode=OneWay}"
               Source="{Binding PrintMat, Converter={cnvt:MatToWriteableBitmapConverter}, ConverterParameter=Bgr24}"
               Stretch="Fill" />
    </Grid>
</UserControl>
